name: test

channels:
  - conda-forge
  - defaults
  - samoturk

dependencies:

    # Base depends
  - python>=3.6
  - pip
    # Package dependencies
  - biopandas
  - biopython
  - numpy
  - nglview
  - scipy
  - matplotlib
  - seaborn
  - jupyter
  - jupyterlab
  # Testing
  - pytest
  - pytest-cov
  - codecov
  # Docs
  - sphinx
  - sphinx_rtd_theme
  - nbsphinx

  # Remove when opencadd is conda-installable
  - rdkit
  - bravado
  - tqdm

  - pip:
    - https://github.com/volkamerlab/opencadd/archive/add_io_klifs_subpockets.tar.gz

